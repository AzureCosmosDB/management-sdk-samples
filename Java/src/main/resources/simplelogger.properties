# SLF4J SimpleLogger configuration
# This sample keeps user-visible logging at INFO, while suppressing noisy Azure Identity
# credential-chain probing logs.

org.slf4j.simpleLogger.defaultLogLevel=info

# Suppress "Attempted credential ... is unavailable" logs.
org.slf4j.simpleLogger.log.com.azure.identity=warn
